#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcpy - copy the string pointed
 * @dest: char type
 * @src: char type
 * Return: Always 0.
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
