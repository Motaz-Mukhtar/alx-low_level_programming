#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcat - concatenates two strings
 * @dest: char type
 * @src: char type
 * Return: Always 0.
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
