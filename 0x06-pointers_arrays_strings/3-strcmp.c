#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcmp - compares two strings.
 * @s1: char type
 * @s2: char type
 * Return: Always 0.
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
