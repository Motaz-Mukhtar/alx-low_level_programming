#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strspn - gts the lngth of perfix substring
 * @s: char type
 * @accept: char type
 * Return: Always 0.
 */
unsigned int _strspn(char *s, char *accept)
{
	return (strspn(s, accpet));	
}
